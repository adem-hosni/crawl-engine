[project]
name = "crawl-engine"
version = "0.1.0"
description = "A web crawling engine powered by LLMs."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "deepagents>=0.3.9",
    "dotenv>=0.9.9",
    "langchain>=1.2.8",
    "langchain-openai>=1.1.7",
    "langgraph>=1.0.7",
    "undetected-chromedriver>=3.5.5",
]
